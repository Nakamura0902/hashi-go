# これらを設定すると本物のMapbox地図 / Supabase DBに切り替わる。
# 未設定の場合はモック地図 + localStorage で動作する（トークン不要で即起動）。

# Mapbox（地図）。未設定ならモック地図にフォールバック
NEXT_PUBLIC_MAPBOX_TOKEN=

# Supabase（DB）。未設定なら localStorage のモックデータで動作
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
